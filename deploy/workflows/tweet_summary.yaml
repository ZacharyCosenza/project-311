apiVersion: argoproj.io/v1alpha1
kind: CronWorkflow
metadata:
  name: tweet-summary
  namespace: argo
spec:
  schedules:
    - "15 8 * * 1"          # Mondays only, 8:15 AM America/New_York — after that day's inference run
  timezone: "America/New_York"
  concurrencyPolicy: Forbid # skip if a run is already in progress
  workflowSpec:
    serviceAccountName: argo
    entrypoint: tweet-summary
    volumes:
      - name: data
        hostPath:
          path: /app/data
    templates:
      - name: tweet-summary
        container:
          image: ghcr.io/zacharycosenza/modeling:latest
          imagePullPolicy: Always
          command: ["kedro", "run", "--pipeline", "tweet_summary"]
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
          env:
            - name: KEDRO_ENV
              value: "prod"
            - name: HOME
              value: "/tmp"
          envFrom:
            - secretRef:
                name: twitter-credentials
          volumeMounts:
            - name: data
              mountPath: /app/data
